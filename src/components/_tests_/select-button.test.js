import React from 'react';
import {shallow, mount} from 'enzyme'

import {SelectButton} from '../select-button'

describe('<SelectButton />', () => {
  it('Renders without crashing', () => {
    shallow(<SelectButton />)
  })


})
